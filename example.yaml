title: outlier courses
desc: All outlier courses
details:
    endpoint: https://jsonplaceholder.typicode.com
    headers:
        - header: Authorization
          key: bearer
          value: token
    path: /todos
    method: GET
response:
    type: json
    file: courses
    folder: ./response/
    permission: 0644

---

title: outlier courses
desc: All outlier courses
details:
    endpoint: https://jsonplaceholder.typicode.com
    headers:
        - header: Authorization
          key: bearer
          value: token
    path: /todos
    method: GET
response:
    type: browser
    port: 3000
